<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
	<div class="container-fluid">

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="#">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>


				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle show" data-bs-toggle="dropdown" href="{{ path('app_women_at_work_index') }}" role="button" aria-haspopup="true" aria-expanded="true">Women at work</a>
					<div class="dropdown-menu show" data-bs-popper="static">
						<a class="dropdown-item" href="{{ path('app_women_at_work_ml') }}">
							statistique
						</a>

					</div>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_departement_index') }}">Departements</a>
				</li>

				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}

						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_employee_index') }}">Employees</a>
						</li>

						<li class="nav-item">
							<a class="nav-link" href="{{ path ('app_intern_index')}}">
								stagiaires
							</a>

						</li>

						<li class="nav-item">
							<a class="nav-link" href="{{ path ('app_dept_manager_index')}}">Managers</a>
						</li>

						<li>
							<a class="nav-link" href="{{ path ('app_mission_index')}}">
								Missions
							</a>
						</li>
					{% endif %}


			{% if is_granted('ROLE_USER') %}
 <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Mon espace</a>
        <div class="dropdown-menu">
            <a class="dropdown-item" href="{{ path('employee_profil', {'id': app.user.id}) }}">Profil</a>
            <a class="dropdown-item" href="{{ path('app_employee_affichermesprojets', {'id': app.user.id}) }}">Mes projets</a>
            <a class="dropdown-item" href="{{ path('app_employee_affichemesstagiaires', {'id': app.user.id}) }}">Mes stagiaires</a>
            <a class="dropdown-item" href="{{ path('app_mission_mesmissions', {'id': app.user.id}) }}">Mes missions</a>
        </div>
    </li>
{% endif %}
				{% endif %}


			</ul>

		</div>
	</div>
</nav>
