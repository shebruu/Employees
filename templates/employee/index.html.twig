{% extends 'base.html.twig' %}

{% block title %}Employee index{% endblock %}

{% block body %}

 {% if app.user %}

    <h1>Employee index</h1>

    <table class="table">
        <thead>
            <tr>
            
                <th>FirstName</th>
                <th>LastName</th>
              
             
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
          
      
   
            {% if employees is not empty %}
                {% for employee in employees %}
                    <tr>
                
                
                        <td>{{ employee.firstName }}</td>
                        <td>{{ employee.lastName }}</td>
                    
                        
                        <td>{{ employee.email }}</td>
                        <td>

                           
                            {% if app.user and app.user.isAdmin %}
                                <a href="{{ path('app_employee_edit', {'id': employee.id}) }}">edit</a>
                           
                           <a href="{{ path('app_employee_show', {'id': employee.id}) }}">show</a>
                               
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            {% else %}
                <tr>
                    <td colspan="9">no records found</td>
                     <p>Veuillez <a href="{{ path('login_route') }}">vous connecter</a> ou <a href="{{ path('register_route') }}">créer un compte</a>.</p>
                </tr>
            {% endif %}
        </tbody>
    </table>

    {% if app.user and app.user.isAdmin %}
        <a href="{{ path('app_employee_new') }}">Create new</a>
    {% endif %}

    {% endif %}
{% endblock %}

