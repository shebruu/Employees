<form method="post" action="{{ path('app_employee_delete', {'id': employee.emp_no}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.emp_no) }}">
    <button class="btn">Delete</button>
</form>
