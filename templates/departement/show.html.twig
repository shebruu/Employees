{% extends 'base.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Departement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Dept_no</th>
                <td>{{ departement.deptNo }}</td>
            </tr>
            <tr>
                <th>Dept_name</th>
                <td>{{ departement.deptName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ departement.description }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ departement.address }}</td>
            </tr>
            <tr>
                <th>Roi_url</th>
                <td>{{ departement.roiUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_departement_index') }}">back to list</a>

    <a href="{{ path('app_departement_edit', {'id': departement.id}) }}">edit</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
