{% extends 'base.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Employee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee.id }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ employee.birthDate ? employee.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ employee.firstName }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ departement.deptName }}</td>
            </tr>
            <tr>
          
        </tbody>
    </table>

<h4> historique des employés</h4>
<ul>
{% for employee in departement.employees %}
<li> {{ employee.id }} {{ employee.firstName }} {{ employee.lastName }}</li>
{% endfor %}

</ul>
    <a href="{{ path('app_departement_index') }}">back to list</a>

    <a href="{{ path('app_departement_edit', {'id': employee.id}) }}">edit</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
